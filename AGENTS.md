# CPQ Agent 仓库工作约定

## 仓库与分支

- 本仓库是 `cpq_agent`，不再把 `cad_engine` 作为本项目的 GitLab 跟踪来源。
- 日常开发和发布统一使用 `20260909` 分支；该分支以 GitHub 的 `yiweilineng4` 为初始基线。
- 日常修改只进入开发分支 `20260909`；`master` 是唯一发布主线，只能通过 `20260909 → master` Merge Request 合入，禁止直接 push、自动合并或 force push。
- 遗留 `main` 只作旧 `cad_engine` 历史保留，不得作为开发、MR、tag、Release 或部署来源；未经用户再次明确要求不得删除或改写。
- 拉取以 GitHub `origin/20260909` 为首选源；GitHub 分支暂不可用时可从 `gitlab/20260909` 快进同步，禁止自动合并或改写历史。
- 用户明确要求 push/推送时，必须把同一个提交推送到 GitHub 与 GitLab 的 `20260909`。推送前先运行 `python3 scripts/push_remotes.py --check`，再运行 `python3 scripts/push_remotes.py`；任一远端失败都必须如实报告并以同一 HEAD 重试。用户未明确要求 push 时不得自行推送。
- GitHub：`git@github.com:tianzj890107/cpq_agent.git`
- GitLab：`git@gitlab.boulderaitech.com:ai-team/cpq_agent.git`
- 当前内网服务 `http://172.16.10.34:8010/` 对应本仓库服务；除非用户明确要求，不要改用旧 `cad_engine` 部署链路。

## Spec、红测与 DeepSeek 实现边界

- 收到功能或缺陷需求后，Codex 先写清楚可验收的 spec，再补充能复现缺口且在实现前失败的自动化测试（red test）。
- 红测必须实际运行，并记录预期失败点；如果测试意外通过，应先修正测试，使它确实覆盖需求缺口。
- Codex 不直接编写业务实现。红测确认后，Codex提供一份可直接交给 DeepSeek 的实现提示词，至少包括：目标、相关上下文、允许修改范围、禁止事项、验收标准和测试命令。
- 实现提示词只在会话中交付，不在仓库创建 `prompts/` 或其他提示词目录/文件，除非用户再次明确要求落盘。
- 业务实现由用户安排 DeepSeek 完成。DeepSeek 返回实现后，Codex负责代码审查、运行测试和核对 spec；发现问题时继续给出针对 DeepSeek 的修正提示词，不越过该边界自行补业务实现。
- 仅限测试脚手架、spec、提示词、仓库配置和用户明确授权 Codex 修改的非业务文件，可由 Codex直接修改。

## 提交与推送检查

- 不提交密钥、本地配置、历史会话、数据库运行文件、缓存或 `.DS_Store`。
- 提交前检查 `git status`、目标 diff 和相关测试结果；不得覆盖用户已有的未提交修改。
- 推送后用 `git ls-remote` 分别检查 GitHub/GitLab 的 `refs/heads/20260909`，两端必须指向当前提交。
- 只暂存本任务相关文件，禁止用 `git add -A` 把无关修改一起提交。检查、测试或推送失败时不得谎报完成。
- 推送失败时保留已成功的远端结果，修复外部阻塞后以同一 HEAD 只补推缺失远端；不得生成补偿 commit、force push、改写历史或改走服务器部署绕过仓库权限。

## 周 changelog

- changelog 永久只按周维护（以自然周为周期），不再创建日报，也不需要用户逐周显式开启。文件统一放在 `changelog/changelog_M_D_D.md`，覆盖周一至周五，例如本周为 `changelog/changelog_9_7_11.md`；跨月使用 `changelog_M1_D1_M2_D2.md`。
- 每次实际修改代码、配置、规则、文档、测试、脚本或构建产物后自动同步当周文件；按用户可见能力和最终状态合并记录，并注明日期，不记录排查过程或被后续覆盖的中间方案。
- 更新前检查当周提交、工作区 diff、现有周记录及相关部署/测试文档。运行日志、缓存、会话、上传文件和数据库数据不写入 changelog。
- 纯查看、解释或诊断且未修改仓库时不更新 changelog；历史周记录中的旧流程描述作为历史事实保留，不机械改写。

## Push、Merge、Tag、Release、部署边界

- `push`：提交验证后的 `20260909`，推送 GitHub 和 GitLab；不创建 MR、tag、Release，不部署。
- `merge`：只创建或更新 GitLab MR `20260909 → master`，reviewer 固定 `tianzijing`，assignee 固定 `zhangzhen`；不得自动合并。
- `tag`：只有用户明确指定版本号并明确要求打 tag 时，才允许在已合入 `master` 的提交上创建 annotated tag；禁止移动已有 tag，禁止 `git push --tags`。
- `release`：只有用户明确要求创建 GitLab Release 时执行；必须绑定已存在且已推送的同名 tag。Release 不等于部署。
- `部署`：只有用户在当前任务明确授权具体环境和版本时执行。部署前校验目标 tag/commit，保护服务器设置和历史数据，部署后检查 `http://127.0.0.1:8010/`；push、merge、tag、Release 均不得自动部署。
- CI 只允许在 Merge Request 和 `master` 上做测试/镜像构建验证；CI 中禁止部署、重启服务器和写生产环境。

## 任务指令与动作映射（最高优先级）

- 用户说 `push`/“推送”：只提交已验证的当前任务并将同一 HEAD 推送到 GitHub/GitLab `20260909`；不创建 MR、tag、Release，不部署。
- 用户说 `merge`/“合并”/“创建 MR”：只创建或更新 `20260909 → master` MR，完整归纳 `master..20260909` 的提交和 diff，等待 `tianzijing` code review；不得自动合并或直接 push `master`。
- 用户说 `tag`：只有同时明确给出目标版本号时才创建并推送该单个 immutable annotated tag；不创建 Release，不部署。
- 用户说 `Release`：只基于已存在的同名远端 tag 幂等创建/更新 GitLab Release；不移动 tag，不部署。
- 用户说“部署”：只部署当前指令明确的环境和 tag/commit；没有明确目标时先停止并询问，不从历史授权、文档或建议推断。
- 用户只说“修复”“完成”“验收”“发布代码”或“做好”：这些词不自动授权 push、MR、tag、Release、服务器写操作或部署。

## 任务终态与“继续”指令（最高优先级）

- 用户仅说“继续”时，只能继续尚未完成且此前已获授权的当前任务；不得重开已完成任务或扩大交付范围。
- 一旦上一轮明确报告“已完成”“已提交”“已推送”“验收完成”或其他终态，该任务即关闭。
- 终态回复中的遗留风险、可选建议、未做的人工检查和“如需可继续”只是信息，不是待执行队列，也不构成授权。
- 任务关闭后用户只说“继续”时，不得调用工具、运行命令、修改文件、测试、启动服务、浏览器验收、提交、推送、创建 MR、tag、Release 或部署；只回复上一任务已完成并等待具体指令。
- 只有任务明确仍在进行中时，“继续”才表示恢复已经授权但未完成的步骤。无法判断是否仍在进行中时，按已完成处理并等待明确指令。
- 用户必须点名新的具体动作，才能在已关闭任务后开始工作；不得从旧对话、旧提示词、README、changelog、DEPLOYMENT 或脚本注释推断新授权。

## 版本与发布安全

- Agent 不得自行决定或升级正式版本；必须由用户明确指定 `vMAJOR.MINOR.PATCH`。自然周、commit、push、MR 或部署次数均不自动改变版本号。
- 指定版本号不等于授权 tag；授权 tag 不等于授权 Release；授权 Release 不等于授权部署；授权部署不等于授权升级版本。
- 正式版本文档只在用户明确要求定版时创建，并同步版本索引和当周 changelog。已定版版本文档与 tag 不得覆盖、移动或强制更新。
- Release 或 tag 部分成功时保留成功结果，修复后只补缺失步骤；禁止删除重建、`git push --tags` 或 force push。

## 服务器、服务与外部状态授权

- 默认禁止 SSH/SCP/rsync、服务器 `git pull`、线上文件修改、容器操作、`systemctl`、停止/启动/重启进程和服务。只有用户在当前任务明确要求对应服务器或服务操作时才执行，历史授权不延续。
- 本地服务启动/停止、浏览器人工验收、GitLab 项目设置修改、分支删除和 CI/CD 设置修改也需要用户当前明确指令；代码实现或 push 不自动授权这些动作。
- 文档中出现部署命令不构成执行授权。push、MR、CI build、tag、Release 和部署始终互相独立。
- CI/pipeline 处于 pending、failed 或没有 Runner 时不得声称通过，也不得自行修改 Runner、项目 CI/CD 设置或生产环境来绕过；只有用户明确要求处理 CI 时才操作。

## 历史会话、业务数据与删除安全（最高优先级）

- 永远不得擅自删除、清空、迁移或覆盖历史会话、项目、任务、附件、模型设置、上传文件、数据库记录和服务器持久化目录。测试和验收不得调用删除/重置接口清理生产数据。
- 用户明确要求删除时，仍须先解析完整精确目标并说明影响；涉及历史会话或生产业务数据必须在执行前再次确认。优先使用业务 API 和可恢复方式，不直接操作底层存储。
- 禁止用空值、未定义变量、宽泛 glob 或未经校验的路径执行递归删除；禁止把仓库根、数据根、home、`/` 或其上级目录作为删除目标。
- 删除前至少执行两项防御检查：验证目标格式、确认目标存在且不是根目录、dry-run 列出范围、建立可恢复备份。删除后复核相邻数据和服务状态。
- 分支、tag、Release、远程仓库内容的删除或历史改写同样需要当前明确授权；一次性删除授权完成、取消或任务终止后立即失效，不得复用。

## 完成前检查

- 报告完成前确认当周 changelog 已准确同步（纯只读任务除外），并检查 `git status`、目标 diff、`git diff --check`、相关测试/构建/语法检查。
- 无法运行的检查必须说明具体原因；不得把未执行、失败、pending 或仅静态检查说成通过。
- 最终回复必须区分：本地修改、已提交、GitHub 已推送、GitLab 已推送、已创建 MR/tag/Release、已部署与已验证；不得用“已完成”掩盖部分失败。
