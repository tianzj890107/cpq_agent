# CPQ suite runtime deps (versions pinned to the verified open-claude/.venv).
# ASCII-only comments on purpose: pip on a cp1252 Windows console fails to read
# UTF-8 (CJK) comments and aborts the whole install. Keep this file ASCII.
# The open_claude engine itself is NOT pip-installed -- each server references
# the open-claude/ source dir via sys.path.
anthropic==0.116.0
# Bumped 2.44.0 -> 2.45.0: tech_app's requirements declare openai>=2.45.0 and both
# share one interpreter, so 2.44 left the image below the declared floor.
openai==2.45.0
psycopg[binary]==3.3.4
openpyxl==3.1.5
pdfplumber==0.11.10
pypdf==6.14.2
PyMuPDF==1.26.4
python-docx==1.2.0
rich==15.0.0
prompt_toolkit==3.0.52

# ---- tech_app: embedded process_drawing full-chain FastAPI app -------------
# The 4th assistant ("tech process") is the process_drawing app under tech_app/,
# launched by cpq_suite_server as a uvicorn subprocess and reverse-proxied on 8010.
# Extra deps below are what it needs on top of the CPQ suite. anthropic / openai /
# pypdf are already pinned above and satisfy tech_app's floors (>=0.69 / >=2.45 /
# >=5,<7), so they are NOT duplicated here (one version per package).
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
sqlalchemy==2.0.36
python-dotenv==1.0.1
pydantic==2.10.4
# qwen_client now builds its own httpx.Client (proxy / trust_env control), so httpx
# is a direct dependency rather than something inherited from the SDKs.
httpx>=0.27,<1
# backend/time_utils.py resolves ZoneInfo("Asia/Shanghai"); the slim base image has
# no system tz database, so without this every timestamped record raises at import.
tzdata>=2025.1
# cadquery drives geometry generation (STEP/STL) only. It is heavy and needs OCCT
# runtime libs (libgl1 libglu1-mesa libxrender1 libxext6 libsm6). The LLM-driven
# process-evaluation / drawing-parse / report chain works without it (guarded:
# generate-geometry returns 503 when absent). Uncomment + add OCCT libs to enable.
# cadquery==2.5.2
