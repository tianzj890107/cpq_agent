function reportFields(r={}) {return `<div class="field"><label>报告标题 <em>*</em></label><input id="reportTitle" value="${esc(r.title||'')}" placeholder="请输入工艺评估报告标题"></div><div class="field"><label>执行摘要</label><textarea id="reportOverview" placeholder="概述项目、范围和评估结论">${esc(r.overview||'')}</textarea></div><div class="inline-fields"><div class="field"><label>关键评估结果（每行一项）</label><textarea id="reportHighlights" placeholder="工艺路线已明确\n关键尺寸需复核">${esc((r.highlights||[]).join('\n'))}</textarea></div><div class="field"><label>风险与待办（每行一项）</label><textarea id="reportRisks" placeholder="供应周期待确认\n清洁度要求待补充">${esc((r.risks||[]).join('\n'))}</textarea></div></div><div class="field"><label>综合结论</label><textarea id="reportConclusion" placeholder="请输入是否建议进入下一阶段的结论">${esc(r.conclusion||'')}</textarea></div>`}
function readReport(r){const lines=id=>document.querySelector(id).value.split('\n').map(x=>x.trim()).filter(Boolean);return {...r,title:document.querySelector('#reportTitle').value.trim(),overview:document.querySelector('#reportOverview').value.trim(),highlights:lines('#reportHighlights'),risks:lines('#reportRisks'),conclusion:document.querySelector('#reportConclusion').value.trim()};}
async function getReport(){return (await api(`/api/projects/${projectId}/process-report`)).report;}
async function renderSummary(){if(!projectId){location.href='home.html';return;}let r=await getReport();if(!r){document.querySelector('#app').innerHTML=`${header()}${workflow(3,'3.1')}<section class="title-card card"><div class="title-row"><h1>汇总工艺评估结果</h1><span class="badge gray">尚未生成草稿</span></div><div class="ai-hint"><strong>▣ 数据汇总</strong><span>将读取已保存的图纸解析、技术工艺与总结数据生成报告草稿；不会调用模型。</span></div></section><section class="card section"><h2>尚未生成工艺评估报告</h2><p style="color:var(--muted)">请先确认需求、完成必要的图纸解析与技术工艺数据填写。系统会按现有保存内容创建可编辑报告草稿。</p><button class="btn primary" id="prepareReport">生成报告草稿</button></section>`;await loadMe();document.querySelector('#prepareReport').onclick=async()=>{try{await api(`/api/projects/${projectId}/process-report/prepare`,{method:'POST'});renderSummary();}catch(e){toast(e.message,4200)}};return;}document.querySelector('#app').innerHTML=`${header()}${workflow(3,'3.1')}<section class="title-card card"><div class="title-row"><h1>汇总工艺评估结果</h1><span class="badge ${statusClass(r.status)}">${statusLabel(r.status)}</span><span style="color:var(--muted);font-size:12px">${esc(r.report_no)} · V${r.version}</span></div><div class="ai-hint"><strong>▣ 报告草稿</strong><span>内容来自当前项目已保存的真实数据。保存和送审均不会重复调用图纸 AI。</span></div></section><section class="card section"><h2>工艺评估报告</h2>${reportFields(r)}</section><section class="grid"><section class="card section"><h2>来源数据状态</h2>${sourceMetrics(r.source_snapshot||{})}</section><section class="card section"><h2>下一步</h2><p style="color:var(--muted)">确认报告内容后送审。审核通过后才可进入发布和分发。</p><a class="btn secondary" href="${href('report.html')}">查看图纸解析报告</a></section></section><section class="card section"><h2>报告留痕</h2>${renderHistory(r.history)}</section><div class="footer-actions"><div class="footer-actions-inner"><a class="btn secondary" href="${href('index.html')}">上一步</a><div class="button-row"><button class="btn secondary" id="saveReport">保存草稿</button><button class="btn primary" id="submitReport">提交审核</button></div></div></div>`;await loadMe();document.querySelector('#saveReport').onclick=()=>saveReport(r,false);document.querySelector('#submitReport').onclick=()=>saveReport(r,true);}
function sourceMetrics(s){const ir=s.ir||{}, steps=s.steps||{};const completed=Object.values(steps).filter(x=>x&&Object.keys(x).length).length;return `<div class="metric-grid"><div class="metric"><b>${(ir.parts||[]).length}</b><span>已识别零件</span></div><div class="metric"><b>${completed}</b><span>已保存工艺模块</span></div><div class="metric"><b>${(s.summary?.risks||[]).length}</b><span>汇总风险项</span></div></div><p style="color:var(--muted);font-size:12px">数据快照在生成草稿时固化；如需刷新，请在草稿状态重新生成。</p>`;}
async function saveReport(r,submit){try{const next=readReport(r);if(!next.title)return toast('请填写报告标题');const saved=(await api(`/api/projects/${projectId}/process-report`,{method:'PUT',body:JSON.stringify(next)})).report;if(submit){await api(`/api/projects/${projectId}/process-report/submit-review`,{method:'POST',body:JSON.stringify({comment:'评估报告已汇总，提交审核。'})});location.href=href('report-review.html');}else{toast('报告草稿已保存');}}catch(e){toast(e.message,4200)}}
async function renderReportReview(){if(!projectId){location.href='home.html';return;}const r=await getReport();if(!r){location.href=href('summary.html');return;}document.querySelector('#app').innerHTML=`${header()}${workflow(3,'3.2')}<section class="title-card card"><div class="title-row"><h1>审核工艺评估报告</h1><span class="badge ${statusClass(r.status)}">${statusLabel(r.status)}</span></div><div class="ai-hint"><strong>▣ 报告审核</strong><span>审核人可通过或退回报告。审批结果会锁定在流程记录中，发布前不可修改。</span></div></section><section class="grid"><section class="card section"><h2>${esc(r.title)}</h2><div class="field"><label>执行摘要</label><div>${text(r.overview||'—')}</div></div><div class="field"><label>关键结果</label><div>${(r.highlights||[]).length?r.highlights.map(x=>`• ${esc(x)}`).join('<br>'):'—'}</div></div><div class="field"><label>风险与待办</label><div>${(r.risks||[]).length?r.risks.map(x=>`• ${esc(x)}`).join('<br>'):'—'}</div></div><div class="field"><label>综合结论</label><div>${text(r.conclusion||'—')}</div></div></section><section class="card section"><h2>审核意见</h2><textarea id="reviewComment" placeholder="请填写审核意见或退回修改要求"></textarea><p class="notice">只有 reviewer 或 admin 角色可以审核。当前权限由服务端强制校验。</p></section></section><section class="card section"><h2>报告留痕</h2>${renderHistory(r.history)}</section><div class="footer-actions"><div class="footer-actions-inner"><a class="btn secondary" href="${href('summary.html')}">上一步</a><div class="button-row"><button class="btn danger" id="rejectReport">退回修改</button><button class="btn primary" id="approveReport">审核通过</button></div></div></div>`;await loadMe();const act=async decision=>{try{const comment=document.querySelector('#reviewComment').value;if(!comment.trim())return toast('请填写审核意见');await api(`/api/projects/${projectId}/process-report/review`,{method:'POST',body:JSON.stringify({decision,comment})});location.href=decision==='approve'?href('report-publish.html'):href('summary.html');}catch(e){toast(e.message,4200)}};document.querySelector('#approveReport').onclick=()=>act('approve');document.querySelector('#rejectReport').onclick=()=>act('reject');}
async function renderPublish(){if(!projectId){location.href='home.html';return;}const r=await getReport();if(!r){location.href=href('summary.html');return;}document.querySelector('#app').innerHTML=`${header()}${workflow(3,'3.3')}<section class="title-card card"><div class="title-row"><h1>发布工艺评估报告</h1><span class="badge ${statusClass(r.status)}">${statusLabel(r.status)}</span></div><div class="ai-hint"><strong>▣ 发布与分发</strong><span>发布后将记录发布人、时间、分发对象和说明。不会使用虚构收件人。</span></div></section><section class="grid"><section class="card section"><h2>待发布报告</h2><div class="field"><label>报告编号</label><div>${esc(r.report_no)}</div></div><div class="field"><label>报告标题</label><div>${esc(r.title)}</div></div><div class="field"><label>审核结论</label><div>${text(r.review_note||'已审核通过')}</div></div><a class="btn secondary" href="${href('report-review.html')}">返回审核页</a></section><section class="card section"><h2>分发对象</h2><div class="field"><label>姓名 / 角色（每行一位）</label><textarea id="recipientNames" placeholder="例如：张三 / 工艺工程师\n李四 / 项目经理">${esc((r.recipients||[]).map(x=>`${x.name}${x.organization?' / '+x.organization:''}`).join('\n'))}</textarea></div><div class="field"><label>发布说明</label><textarea id="publishComment" placeholder="填写分发范围、版本说明或后续要求"></textarea></div></section></section><section class="card section"><h2>报告留痕</h2>${renderHistory(r.history)}</section><div class="footer-actions"><div class="footer-actions-inner"><a class="btn secondary" href="${href('report-review.html')}">上一步</a><button class="btn primary" id="publishReport">发布工艺评估报告</button></div></div>`;await loadMe();document.querySelector('#publishReport').onclick=async()=>{try{const recipients=document.querySelector('#recipientNames').value.split('\n').map(x=>x.trim()).filter(Boolean).map(x=>{const [name,organization='']=x.split('/').map(y=>y.trim());return {name,organization};});if(!recipients.length)return toast('请至少填写一位分发对象');const comment=document.querySelector('#publishComment').value;await api(`/api/projects/${projectId}/process-report/publish`,{method:'POST',body:JSON.stringify({comment,recipients})});toast('报告已发布并完成分发留痕');setTimeout(()=>location.href=href('requirement-detail.html'),700);}catch(e){toast(e.message,4200)}};}
if(location.pathname.endsWith('summary.html'))renderSummary().catch(e=>toast(e.message,4200));if(location.pathname.endsWith('report-review.html'))renderReportReview().catch(e=>toast(e.message,4200));if(location.pathname.endsWith('report-publish.html'))renderPublish().catch(e=>toast(e.message,4200));
