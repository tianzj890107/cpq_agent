function renderReview(req){const d=req.data||{};document.querySelector('#app').innerHTML=`${header()}${workflow(1,'1.3')}<section class="title-card card"><div class="title-row"><h1>审核工艺评估需求</h1><span class="badge ${statusClass(req.status)}">${statusLabel(req.status)}</span></div><div class="ai-hint"><strong>▣ 审核节点</strong><span>审核通过后，项目可进入图纸解析。审核动作将按当前登录角色实名留痕。</span></div></section><section class="grid"><section class="card section"><h2>待审需求</h2><div class="field"><label>需求编号</label><div>${esc(req.requirement_no)}</div></div><div class="field"><label>需求名称</label><div>${esc(req.title)}</div></div><div class="field"><label>提交内容</label><div>${text(d.description||'—')}</div></div><div class="field"><label>确认说明</label><div>${text(req.confirmation_note||'—')}</div></div><a class="btn secondary" href="${href('requirement-detail.html')}">查看完整需求详情</a></section><section class="card section"><h2>审核意见</h2><div class="field"><label>审核说明 <em>*</em></label><textarea id="reviewComment" placeholder="请填写通过依据或退回修改要求"></textarea></div><div class="notice">只有 reviewer 或 admin 角色可以执行通过 / 退回。鉴权关闭的本地环境会以系统管理员执行。</div></section></section><section class="card section"><h2>流程留痕</h2>${renderHistory(req.history)}</section><div class="footer-actions"><div class="footer-actions-inner"><a class="btn secondary" href="${href('requirement-confirm.html')}">上一步</a><div class="button-row"><button class="btn danger" id="rejectRequirement">退回修改</button><button class="btn primary" id="approveRequirement">审核通过，进入图纸解析</button></div></div></div>`;loadMe();const review=async decision=>{try{const comment=document.querySelector('#reviewComment').value;if(!comment.trim())return toast('请填写审核说明');await api(`/api/projects/${projectId}/requirement/review`,{method:'POST',body:JSON.stringify({decision,comment})});location.href=decision==='approve'?href('index.html'):href('requirement-create.html');}catch(e){toast(e.message,4200)}};document.querySelector('#approveRequirement').onclick=()=>review('approve');document.querySelector('#rejectRequirement').onclick=()=>review('reject');}
