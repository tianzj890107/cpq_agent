
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4


anthropic>=0.69.0
# Responses API 后台任务、JSON 输出与取消支持。
openai>=2.45.0
# OpenAI/Qwen 客户端直接使用的网络与代理传输层；避免只依赖 SDK 的间接依赖。
httpx>=0.27,<1
# zoneinfo 在精简 Docker 镜像或 Windows 环境中读取 Asia/Shanghai 的时区数据库兜底。
tzdata>=2025.1


cadquery==2.5.2

python-dotenv==1.0.1

sqlalchemy==2.0.36
# 技术文档中的 PDF 纯文本提取；扫描件仍需走视觉/OCR 能力。
pypdf>=5.0,<7
# 成本口径的证据类测试（工作簿读取）用；与根 requirements.txt 的版本保持一致。
# 生产引擎不得依赖它（packaging_cost.py 不 import openpyxl）。
openpyxl==3.1.5
