# 配置报价 CPQ 助手 · 使用说明

（2026-07-16 版，对应一体化服务 cpq_suite_server.py）

## 一、系统组成

一个服务、一个端口、三个智能体：

| 组件 | 说明 |
|---|---|
| 首页（报价首页.html） | 系统入口：三个助手的切换标签、新建入口、单据卡片列表、历史会话、模型设置 |
| 报价助手 | 6 步引导式智能报价（确认需求 → 定价 → 加价 → 报价方案 → 输出报价单 → 导入数据库） |
| 配置助手（XBOM） | 由历史 BOM 生成产品配置 BOM，第 6 步人工校对后导入数据库 |
| 规则助手 | 从规则文档抽取规则并自动生成 Groovy 公式，校对后导入数据库 |

业务数据源 = 远程 PostgreSQL（表结构以《亿纬锂能DA梳理.xlsx》本体为准）；大模型走云 API 或本地 OpenAI 兼容网关。

## 二、启动

### 方式 A：直接运行（开发/演示机）

```bash
python cpq_suite_server.py --host 0.0.0.0 --port 8010
```

浏览器访问 `http://<主机>:8010/` 直达首页（根路径直接返回 报价首页.html）。

### 方式 B：Docker（部署机）

```bash
docker build -t cpq-suite .
docker run -d -p 8010:8010 \
  -e DEEPSEEK_API_KEY=sk-xxx \            # 按所用模型传对应 Key
  -e CPQ_PG_HOST=172.16.5.181 -e CPQ_PG_PORT=32444 \
  -e CPQ_PG_USER=postgres -e CPQ_PG_PASSWORD=xxx \
  -e CPQ_PG_DATABASE=metabase -e CPQ_PG_SCHEMA=master_data \
  cpq-suite
```

- 数据库连接全部可用 `CPQ_PG_*` 环境变量覆盖（不传则用镜像内默认值）。
- **API Key 一律运行时传入，绝不写进镜像**。支持：ANTHROPIC / OPENAI / DEEPSEEK / DASHSCOPE(QWEN) / ZHIPUAI(GLM) / MOONSHOT(KIMI)。

## 三、首页操作

1. 顶部三个标签（**报价助手 / 产品配置 / 规则配置**）决定后续进入哪个助手——路由完全由点选的标签决定，输入内容不会改变去向。
2. 在输入框描述需求（可配合「上传文档」附 Excel/Word/PDF/图片），点发送 → 弹出**项目信息确认框**（项目名称、单号自动生成、报价需填客户名称、规则需选规则类型）→ 确认后进入对应助手工作台。
3. 「新建报价/创建配置/创建」按钮 = 进入当前标签助手的空白单（同样先过确认框）。
4. 下方卡片列表展示该助手的真实历史单据（我的/全部切换、搜索、分页），点「详情」直接打开该会话。
5. 右上角可打开**历史记录**（三个助手的会话统一展示，点击跳转对应工作台）和**模型设置**。

## 四、报价助手（6 步）

左侧与助手对话，右侧是工作台（步骤条 + 各步固定表单/表格）。每步助手会自动查库、填推荐值（推荐值有颜色高亮），用户可直接改表格，改完点「确认，进入下一步」。

1. **确认需求配置**：从文档/描述提取测算基本信息、目的地、付款里程碑、物流；产品信息/技术参数由产品参数库匹配（无满足产品则不填）。**步骤末尾会询问是否进行 BOM 匹配**——「确定」进入第 2 步，「取消」跳过直接到第 3 步。
2. **定价-基础成本**：按配单规则生成实例 BOM（层级 L1-Ln），匹配物料成本。
3. **定价-利润加成**：取全规则库「定价」类规则逐条匹配。
4. **报价-其他加价项**：取全「报价」类规则逐条匹配。
5. **报价方案**：汇总计算（跳过第 2 步时基础成本用产品信息「价格」）。
6. **输出报价单** → 点「**导入数据库**」：各分区按 ER 关系写入 clm_calc_* / clm_quote_* 各表（产品信息取第 1-4 步更新的合并结果，BOM 层级按头/行递归落库），逐分区回报成败。

## 五、配置助手（XBOM）

上传某产品的历史 BOM，助手自动走完模块识别、物料归集、规则检查等前 5 步，第 6 步给出**可编辑配置 BOM 表**：

- 每行可改物料编码/名称/数量等，行末下拉为该行选择配置规则（AI 推荐的规则会高亮）；可增删行。
- 树形层级展示（L1-Ln 可展开收起）。
- 点「**导入数据库**」：L1 行自身作为 BOM 头写入 CLM_BASE_INFO（无产品编码的头不会写入），其余行写 CLM_LINE_INFO 并按层级递归挂头；**L1 选的规则写入 clm_base_rule_rel，其他层级的规则写入 clm_line_rule_rel**（按规则名反查规则库 id，查不到会在结果里给出警告）。
- 「生成规则文档」按钮可导出 AI 新推荐（库里没有）的规则，交给规则助手录入。

## 六、规则助手

上传规则文档（或口述），两步合在右侧一张规则表里完成：

1. 助手抽取规则逐行录入（复选/ID/名称/描述列）；
2. 自动为每行生成 Groovy 公式回填末列（生成时会查库取字段依据）。

各列单元格可直接点击编辑、可「+ 新增规则」人工加行；确认后点「**导入数据库**」写入独立的 rule_agent_rules 表。

## 七、模型设置（每个页面右上角齿轮）

- **云模型**：下拉选择（内置 Qwen / DeepSeek 等），填对应提供方 API Key（只保存在本机 settings 文件，不入库、不进 git）。可选自定义 Temperature、最大输出 Tokens、**深度思考**（Claude/DeepSeek 生效，DeepSeek 自动切思考版模型）。
- **本地模型**：选「本地模型」后填 OpenAI 兼容网关 Base URL 和模型名称，可勾「启用思考 enable_thinking」、设截断提示 Tokens；此时**不提供「深度思考」选项**（本地思考只由启用思考控制）。
- **无模型模式**：模型选「无模型」，不调用大模型，右侧表格全人工填写后照常导入数据库（应急/断网演示用）。

## 八、注意事项

- **《亿纬锂能DA梳理.xlsx》是本体语义层**：表单字段、库 schema、表访问边界都从它解析，**改了 xlsx 必须重启服务**才生效。
- 助手查库一律只读（仅 SELECT/WITH 单语句）；报价助手禁止查询 clm_calc_*/clm_quote_*（它们是导入目标不是数据源）；单次查询上限 500 行，超限会自动引导助手分批取数。
- 历史会话存本机磁盘（cpq_history/ xbom_history/ rule_history/），重启不丢；settings 文件含 API Key，注意不要外传。
- 导入数据库成功的气泡里会附带警告明细（如规则名在库里未找到），请留意 ⚠️ 行。
